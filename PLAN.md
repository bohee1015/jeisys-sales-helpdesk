# 작업 계획 (PLAN)

영업 지원 헬프데스크의 **현재 상태와 앞으로 할 일**을 한곳에 모은 문서입니다.
기획 배경은 [PRD.md](./PRD.md), 작업 규칙은 [CLAUDE.md](./CLAUDE.md), 분류 로직 상세는 [docs/분류-로직-가이드.md](./docs/분류-로직-가이드.md)를 봅니다.

> 기준일: 2026-09-17 · 항목을 끝내면 체크하고, 새로 생긴 과제는 해당 우선순위 아래에 추가합니다.

---

## 1. 현재 상태 — 완료

운영 중: https://jeisys-sales-helpdesk.vercel.app (사내 계정 로그인 필요)

| 영역 | 완료된 내용 |
|---|---|
| 접수 | 문장 그대로 입력 → 키워드·규칙 기반 자동 분류(대분류 8 · 소분류 33) → 필수값 자동 추출 · 빠진 값 되묻기 → 선행 업무(기안 승인) 확인 |
| 접수 | "및/그리고"로 이어진 서로 다른 업무 2건 분리 접수, 진행 중 접수 취소, 음성 입력(브라우저 내장) |
| 처리 | 관리자 화면: 요청접수관리 / FAQ 관리 탭, 상태(요청등록 → 처리중 → 완료) 지정, 완료처리자 자동 기록 |
| 처리 | 한 줄 목록 + 상세 팝업(요약 ∣ 원문 분할), 상태·요청자·완료처리자·카테고리 필터 |
| 처리 | 무이자리스 신청 → 사내 견적서 양식 엑셀 자동 생성, 마케팅 물품 요청 → SAP 품목코드 제안 |
| 현황 | 요청 현황판(관리자 전용): 월별 요청자×카테고리 교차표, KPI 4종, 6개월 추이, 24시간 이상 대기 목록 |
| 디자인 | JMAP과 통일(Jeisys 네이비 팔레트 · 공식 로고 · 로그인 화면) |
| 품질 | 선행 업무 예/아니오 오판정, "다음주 X요일" 날짜, 원장님 성함·병원명 오추출 수정 (2026-09-17) |
| 운영 | GitHub 공개 전환(실데이터 가명 처리, 직인 포함 양식 제외), Vercel 배포 |

---

## 2. 다음 작업

### P1 — 분류 로직 안전장치 (분류 결과는 바뀌지 않음)

키워드를 고칠 때 다른 소분류가 깨지는지 바로 확인할 수 있게 하는 것이 먼저입니다. P2는 이 도구가 있어야 안전하게 진행할 수 있습니다.

- [x] **회귀 테스트 명령 `npm run check:nlp`** — `scripts/nlp-cases.json`의 58개 문장으로 분류·필수값 전후 비교 (2026-09-18)
- [x] **키워드 충돌 검사기 `npm run check:keywords`** — 중복·포함 관계·2글자 이하 키워드 표시 (2026-09-18)

### P2 — 분류 정확도 개선 (2026-09-18 점수 체계 개편으로 완료)

- [x] **가중치 점수 체계** — 키워드 1점 + 맥락 패턴(`sig`) 가중치. "장비 하나 잡아주세요" 같은 동사+목적어 표현을 소분류마다 추가
- [x] **점수 보정** — 같은 소분류 안에서 포함 관계인 키워드는 긴 것 1점만
- [x] **범용 키워드 범위 좁히기** — "리스", "변경", "확인" 등은 앞뒤 말과 함께일 때만
- [x] **데모 우선 규칙 완화** — "데모"도 점수 신호로. 출고 신호가 더 세면 출고로
- [x] **품목 여러 개 추출** + 한글 수량("하나" → 1대) + 월 없는 날짜("23일로")
- [x] **규칙 일원화** — 가산점·데모 규칙을 `categories.ts`로, 병원명 제외어는 키워드에서 자동 생성
- [x] **대분류 제안** — 소분류를 못 정해도 대분류가 분명하면 소분류만 고르게
- [ ] 실제 사용 중 나오는 오분류를 `scripts/nlp-cases.json`에 추가하며 계속 보정 (상시)

### P3 — 기능 고도화

- [ ] **현황판 "분류 품질" 블록** — 자동 분류 실패율, 요청당 되묻기 횟수. `helpdesk.requests`에 컬럼 2개(자동분류 여부 · 되묻기 횟수) 추가 필요, 추가 시점부터 집계
- [ ] **무이자리스 견적서 보완** — 접수 항목에 "리스 개월수" 추가 검토, 사업자번호 채우는 방법 설계(현재 수기 입력)
- [ ] **FAQ 자동 응답 확신도** — PRD 5번 "확신도 미달 시 담당자에게 전달" 규칙 구현 (현재는 검색 기반)
- [ ] 현황판 기간 옵션(최근 30일 · 분기), 병원별 요청 순위 — 실제 사용 후 필요하면

---

## 3. 보류 — 검토만 마친 것

| 항목 | 검토 결론 | 다시 볼 조건 |
|---|---|---|
| 모바일 앱 (아이폰 · 갤럭시, 스토어 미등록) | 1단계는 **PWA**(홈 화면 설치)를 권장. iOS 네이티브는 연 1회 인증서 갱신 · Mac 빌드 · 재설치 배포 부담이 큼. PRD 6번(네이티브 앱 비범위)도 함께 고쳐야 함 | 푸시 알림이 꼭 필요하거나 아이폰 설치 팝업이 필요해질 때 |
| 아이폰 음성 입력 (서버 음성 인식) | 사파리에서 브라우저 내장 인식이 불안정. 서버 변환은 요청 내용이 외부로 전송되므로 사내 보안 검토 선행 | 보안 검토 승인 후 |
| 푸시할 때마다 자동 배포 | 현재는 CLI로 수동 배포. Vercel에 GitHub 저장소를 연결하면 자동화 가능 | 배포 빈도가 늘어날 때 |

---

## 4. 결정이 필요한 것

- [ ] **PRD 7번 문구 갱신** — 현재 "사내 전용, 외부 공개·공유 금지". 저장소가 공개됐으므로 "소스 코드는 공개, 요청 데이터는 사내 전용"으로 구분할지
- [ ] **SAP 품목코드 표 공개 유지 여부** — `app/lib/marketingItems.ts` (개인정보는 아님)
- [ ] **로컬 보관 브랜치 정리** — 공개 전 기록은 비공개 보관 저장소에 있으므로 로컬 브랜치 삭제 여부
- [ ] **인수인계 문서 갱신** — `ONBOARDING.md`의 "지금 당장 이어서 할 일"이 배포 이전 기준이라 이 문서로 연결할지

## 5. 운영 체크

- [ ] 자동 커밋·푸시용 GitHub 토큰 만료 전에 재발급해 `.env` 교체 (`repo` 권한)
- 분류 규칙을 고치면 [분류 로직 가이드](./docs/분류-로직-가이드.md)도 함께 수정
- 예시·테스트 데이터는 가명만 사용 (공개 저장소)
